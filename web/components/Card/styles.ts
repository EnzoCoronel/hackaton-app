const wrapper = "w-full relative h-40";
const container = "w-full flex flex-col gap-size-32";
const top = 'w-full top-0';
const bottom = 'w-full bottom-0';

export function loadCardStyles() {
  return {
    wrapper,
    container,
    top,
    bottom,
  };
}
